<div class="row">
  <div class="col-sm-8 coming-soon-col height-sm-full">
    <div class="logo col-sm-6">
      <%= image_tag "logo.svg" %>
    </div>
    <div class="col-sm-9">
      <h3><%= t "coming_soon.description" %></h3>
      <p><%= t "coming_soon.text" %></p>
    </div>
  </div>
  <div class="col-sm-4 coming-soon-col bg-gray-light height-sm-full">
    <h3 class="col-xs-12 bg-gray-light"><%= t "coming_soon.form.title" %></h3>
    <p class="col-xs-12 bg-gray-light"><%= t "coming_soon.form.description" %></p>
    <%= simple_form_for @contact, remote: true, html: { class: "bg-gray-light" } do |f| %>
      <%= f.input :name, placeholder: 'Name', label: false %>
      <%= f.input :email, placeholder: 'Email Address', label: false %>
      <%= f.button :submit, t("coming_soon.form.submit.text"), class: 'btn-primary btn-block' %>
    <% end %>
    <p id="new_contact_status" class="success col-xs-12 bg-gray-light"></p>
  </div>
</div>
