<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to image_tag("logo_no_title.svg", class: 'navbar-brand'), root_path %>
    </div>
    <div class="navbar-right">
      <% if user_signed_in? %>
        <%= link_to current_user.email, edit_user_registration_path %>
        <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-link navbar-btn' %>
      <% else %>
        <%=  link_to 'Login',  new_user_session_path, class: 'btn btn-link navbar-btn' %>
        <%=  link_to 'Sign Up', new_user_registration_path, class: 'btn btn-primary navbar-btn' %>`
      <% end %>
    </div>
  </div>
</nav>
