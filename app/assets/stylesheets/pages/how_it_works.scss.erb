.pages-how_it_works {
  background-color: #f0f0f0;
  color: black;

  @media (min-width: $screen-sm-min) {
    .main-header-holder {
      display: none;
    }
  }

  main {
    @include background-image('shutterstock_229339990_gray_blur', no-repeat scroll center top);
    background-size: contain;
    padding-top: 10%;
  }

  .main-header-holder {
    text-align: center;
  }

  .main-header {
    border-bottom: 1px solid #818181;
    color: $gray-dark;
    display: inline-block;
    font-family: $font-family-body;
    font-size: 1em;
    font-weight: 700;
    letter-spacing: 0.1em;
    line-height: 1.5;
    margin-top: 3em;
    text-align: center;
    text-transform: uppercase;
  }

  .header-ending {
    color: $orange;
  }

  .fade-carousel-holder {
    @media (min-width: $screen-sm-min) {
      padding-top: 4em;
    }

    .swiper-pagination {
      display: none;
    }

    @media (min-width: $screen-xs-min) {
      .swiper-pagination {
        display: block;
      }
    }
  }

  .fade-carousel {
    text-align: center;
    margin-bottom: 3em;

    @media (min-width: $screen-xs-min) {
      margin-bottom: 3em;
      padding-bottom: 4em;
    }
  }

  .step-circle-holder.swiper-pagination-bullet {
    background: transparent;
    border-bottom: 3px solid $gray-light;
    border-radius: 0;
    display: inline-block;
    height: auto;
    opacity: 1;
    padding-bottom: 2em;
    text-align: center;
    width: 25%;

    > h4 {
      display: none;
    }

    @media (min-width: $screen-sm-min) {
      > h4 {
        display: block;
      }
    }

    &.swiper-pagination-bullet-active {
      border-color: $orange;
    }
  }

  .step-circle {
    margin-bottom: 0.25em;
    opacity: 1;

    @media (max-width: $screen-sm-min) {
      width: 60px;
      height: 60px;
      line-height: 60px;
      padding-bottom: 1em;
    }
  }

  .how-it-works-carousel {
    margin-bottom: 2em;
    padding-top: 2em;
    padding-bottom: 2em;
    border-bottom: 1px solid $gray-light;
    transition: 0.5s height;

    &.notransition {
      transition: none;
    }

    h2 {
      text-align: center;
      margin: 0 0 2em;
      font-size: 1.5em;
    }

    @media (min-width: $screen-sm-min) {
      h2 {
        display: none;
      }
    }

    .swiper-slide {
      color: $gray-dark;

      @media (min-width: $screen-sm-min) {
        padding: 0 2em;
      }
    }
  }

  .how-it-works-steps {
    background: white;
    padding-top: 3em;
    padding-bottom: 4em;
    margin-bottom: 6em;
  }
}
