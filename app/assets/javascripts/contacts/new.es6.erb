$(document).on('ready', () =>
{
  $('#new_contact').on('ajax:success', (e) =>
  {
    console.log("[Pyrite] $('#new_contact').on('ajax:success'):", e);
    $('#new_contact_status').text("<%= I18n.t "coming_soon.form.state.email.success" %>")
  })
});
